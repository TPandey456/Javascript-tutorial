console.log(null > 0);
console.log(null == 0);
console.log(null>=0);

//  === it compare the datatypes and also perform comparsion 


// Note

// the reason is that an euality check== and compersion > <>=
//  <= work differnetly.
//  Comphersion conver null to a number, treating it as 0 .
//  that's why (3) null >=0 is true and (1) null > 0 is false.
